'use strict';

angular.module('caseManagerApp.profile', ['ngResource'])

  .controller('ProfileController',
    function ($window, $resource, $mdDialog, $routeParams, $log, $mdToast) {

      // region local var
      var self = this;
      // endregion local var
    });
